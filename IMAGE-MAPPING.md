# 图片素材对应表（贝之然小程序）

## ✅ 素材已经全部导入完成

我从 `D:\跨境电商\A贝歌\素材` 把图片**直接导入并压缩**进了项目。
你**不需要再手动拷图**，打开开发者工具编译即可看到完整界面。

| 项目 | 数量 | 体积 |
|---|---|---|
| 产品图（11 个系列） | 95 张 | 约 3.3 MB |
| 资质证书 | 15 张 | 约 3.4 MB |
| 品牌头图 | 1 张 | 86 KB |
| 底部导航图标（我生成的 SVG） | 6 个 | 2 KB |
| **images 合计** | **119 个** | **约 16.2 MB** |
| **整个小程序包** | 209 个文件 | **约 16.4 MB** |

> ⚠️ 微信主包上限是 **20 MB**。现在 16.4 MB 能传上去，但**余量只有 3.6 MB**。
> 后面如果还要加图，建议用「分包」或把证书再压小一点。压缩参数见文末。

---

## 一、图片放在哪

```
miniprogram/images/
├── products/<系列id>/
│   ├── cover.jpg     系列封面（首页、产品列表用）
│   └── 1.jpg ~ 8.jpg 产品图（详情页轮播）
├── certs/            资质证书（a1-*.jpg、hb-*.jpg、yx-*.jpg、iso-*.jpg）
├── brand/cover.jpg   品牌头图（关于我们页顶部）
└── tab/*.svg         底部导航图标（我生成的，勿删）
```

### 系列 id 对应表

| 系列 | id | 图源 |
|---|---|---|
| 大众化工程内墙系列 | `neiqiang-dazhonghua` | 白底图 `1_20260810_164223_中文_1..8` |
| 地坪漆系列 | `dipingqi` | 白底图 `2_..._1..8` |
| 儿童房系列 | `ertongfang` | 白底图 `3_..._1..8` |
| 防水涂料系列 | `fangshui` | 白底图 `4_..._1..8` |
| 家装内墙系列 | `neiqiang-jiazhuang` | 白底图 `5_..._1..8` |
| 界面剂系列 | `jiemianji` | 白底图 `6_..._1..8` |
| 抗污罩面系列 | `kangwu-zhaomian` | 白底图 `7_..._1..8` |
| 外墙系列 | `waqiang` | 白底图 `8_..._1..8` |
| 无机底固系列 | `wuji-digux` | 白底图 `9_..._1..8` |
| 政府工程内墙系列 | `neiqiang-zhengfu` | 白底图 `10_..._1..8` |
| 墙面剂系列 | `qiangmianji` | `墙面剂系列\export_1 (56/68/69/70/71).png`，**只有 5 张** |

用的是 `贝歌白底图\` 下统一风格的白底产品图（比 `export_1` 那批整齐），
系列封面用的是 `贝歌白底图\贝之然涂料<系列名>.png` 合并版。

---

## 二、还缺 2 张图（需要你补）

**「贝之然」商标注册证目前没有图片。**

素材里只有 PDF，我无法把 PDF 转成图片：

```
贝歌证书电子版和纸质版\贝之然商标注册证\贝之然2类商标注册证67583611(2).pdf
贝歌证书电子版和纸质版\贝之然商标注册证\贝之然35类商标注册证67586855(2).pdf
```

**代码已经做了优雅降级**：这两张卡在「资质与检测」页照常显示标题、编号、机构、结论，
但**不显示「查看原件」按钮**，改为提示：

> 该证书仅有 PDF 原件，如需查看请点击「索取副本」联系我们

如果你想展示图片，做法：
1. 用 PDF 阅读器打开这两个 PDF，截图第一页；
2. 保存为 `miniprogram/images/certs/tm-2.jpg` 和 `tm-35.jpg`；
3. 打开 `miniprogram/data/catalog.js`，把这两条证书里的 `hasImages: false` 删掉。

---

## 三、图片是怎么压缩的（供参考）

原始白底图是 **1024×1024 PNG，单张 1.3–1.7 MB**，95 张就 149 MB，远超上限。
我做了统一压缩：

| 类型 | 处理 | 结果 |
|---|---|---|
| 产品图 | 最长边 800px，JPEG | 单张约 130–180 KB |
| 系列封面 | 最长边 900px，JPEG | 单张约 100–160 KB |
| 品牌头图 | 最长边 1200px，JPEG | 86 KB |
| 资质证书 | 最长边 1500px，JPEG（要看清文字，压得轻） | 单张 115–490 KB |

透明 PNG 转 JPEG 时统一铺了白底，不会发黑。

### 想换压缩参数

`catalog.js` 里 `seriesCover()` / `seriesImage()` 就是拼路径的地方。
如果想改图片尺寸/质量，告诉我目标体积，我重新压缩一遍。

---

## 四、底部导航图标（已生成，勿删）

```
images/tab/home.svg          首页
images/tab/home-on.svg       首页-选中
images/tab/product.svg       产品
images/tab/product-on.svg    产品-选中
images/tab/contact.svg       咨询
images/tab/contact-on.svg    咨询-选中
```

---

## 五、原来那份手动拷贝对照表（已不需要）

保留在 git 历史里即可。现在图片已就位，**这一节你不需要执行任何操作**。

如果你之后想换成别的产品图，只要覆盖同名文件即可，例如把新图存成
`images/products/dipingqi/3.jpg`，详情页第 3 张就会变成新图。
